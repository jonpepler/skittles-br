{
  "name": "skittles-br",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Skittles game — generators ported to TypeScript modules for a static, peer-to-peer build.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
